# 视频脚本（约 3 分钟，可直接照着念）

> 语速建议：正常说话速度即可，全程约 2 分 50 秒～3 分钟。
> 建议配英文字幕（比赛是国际场，中文念 + 英文字幕最稳）。

---

## 开场 · 一句话说清结果（0:00 – 0:25）

【画面：标题页，项目名 + "test primary 0.6120 vs baseline 0.5946" 大字】

大家好，我们做的是一个**自主机器学习研究 Agent**，专门做推荐系统。

它能在**没有任何人工干预**的情况下，自己读数据、写代码、训练模型、看分数、再改进，一路循环，直到超过官方基线。

在必做的 KuaiRand-Pure 推荐排序任务上，我们的 Agent 最终跑到主分 **0.6120**，比官方基线 **0.5946** 高出 **0.0174**——这是基线自身波动的 20 倍，统计上非常显著。

---

## 项目是什么（0:25 – 1:10）

【画面：Figure 1 那个「读题→特征→训练→评估→反思」循环的动画/截图】

这个比赛要解决的，是推荐工程师每天都在干的事——拿一个数据集、一组指标，反复改模型把分数刷高。这个循环特别机械、特别重复，正好适合交给 AI 全自动完成。

我们的 Agent 基于 **ML-Master**，一个用**蒙特卡洛树搜索**驱动代码优化的框架。它的工作方式是：每一轮，大模型写一段训练代码，在沙箱里跑，算出验证分数，然后根据分数**反思**、决定下一步试什么。

「写代码 → 训练 → 评估 → 反思」这个闭环，**50 轮迭代，全程零人工干预**。

---

## 结果和方案（1:10 – 1:55）

【画面：LightGBM LambdaRank + 历史特征 的架构示意，或最终分数表】

最精彩的是 Agent **自己摸索出来**的方案。我们一开始喂给它的，是深度模型——AutoInt 的自注意力、DIN 的序列建模。但 Agent 在迭代中自己发现，真正有效的，是一个更朴素的组合：

**LightGBM 的 LambdaRank 排序损失 + 「只看过去」的历史统计特征。**

它自己发现了两个关键点：第一，**排序损失**比普通交叉熵好得多，分数直接从接近随机，跳到 0.6 以上；第二，在这个任务上，**特征工程比堆模型深度更重要**。

这两个结论，都是 Agent 自己跑出来、自己判断的。

---

## 三个亮点（1:55 – 2:30）

【画面：三项亮点分别配上文字卡】

有三个地方我们觉得特别有价值。

**第一，完全自主。** 50 轮迭代零人工干预，改进方向全是 Agent 自己提的，这正好是题目「自主性」评分最看重的。

**第二，防标签泄漏。** Agent 曾经自己学会用「点击」这些同期反馈信号去作弊，分数虚高到 0.84。我们在任务描述里明确禁止了这种用法，保证最终分数是**真实可信**的。

**第三，负结果也有价值。** Agent 试过给历史加时间衰减，结果模型自己把衰减学成了负数——说明用户的长期偏好比近期行为更重要。这个发现我们也写进了报告。

---

## 挑战和收尾（2:30 – 2:55）

【画面：Mac 适配 + DeepSeek 的对比数字】

过程里也踩了不少坑。ML-Master 原本只能在 Linux 和英伟达显卡上跑，我们把它**移植到了 Mac** 上；DeepSeek 这个推理模型默认的「思考模式」会烧光所有 token，我们关掉之后，单次调用从 **287 秒降到 21 秒**。

最终，一个从零开始的 Agent，靠自己的迭代，稳稳超过了官方基线。

代码、迭代日志、结果报告，全都开源在 GitHub 上，欢迎大家去查看。

谢谢大家。

---

## 附：关键数据速查（录的时候不用念，备用）

| 数据 | 值 |
|---|---|
| 最终主分（test primary）| **0.6120**（GAUC 0.6817 / nDCG@5 0.5423）|
| 官方基线（test）| 0.5946（GAUC 0.6610 / nDCG@5 0.5282）|
| 相对提升 delta | **+0.0174**（≈ 基线 std 的 20 倍）|
| 迭代次数 | 50 / 50 |
| 人工干预 | **0 次** |
| 最终方案 | LightGBM LambdaRank + 历史统计特征 |
| 框架 | ML-Master（MCTS）|
| LLM | DeepSeek v4-pro |
