# PPT 演讲稿（逐页，可直接照着念）

> 对应 11 页 PPT，正常语速约 4–5 分钟。每页开头标了【第 X 页】，翻到对应页再念。

---

## 【第 1 页 · 封面】

大家好，我们是徐诺言和绍渤涵。今天介绍我们的项目——一个用于推荐系统的**自主机器学习研究 Agent**。

核心成果一句话：在 KuaiRand-Pure 这个任务上，我们的 Agent 完全靠自己迭代，最终在测试集拿到主分 **0.6120**，超过官方基线 **0.5946**，提升了 **0.0174**，相当于基线自身波动的 20 倍。

---

## 【第 2 页 · 问题背景】

先看背景。推荐系统的工程师，每天大部分时间都耗在同一个循环上：读数据、看分布、做特征、训练模型、评估，然后根据结果反思、再改。

这个循环高度机械、重复，而且**每一步都在产生和修改代码**——这恰好是代码生成型大模型最擅长自动化的事。所以这个比赛要我们做的，就是把这个循环交给一个 AI Agent 全自动跑。

---

## 【第 3 页 · 方案】

我们的方案，是基于 **ML-Master** 框架做的自主 Agent。它的工作方式是四步：

每一轮，大模型 DeepSeek 写一段训练代码；在沙箱里执行，算出验证分数；第二个模型读日志、判断有没有 bug、提取分数；最后由**蒙特卡洛树搜索**决定下一步——是重新想方案、改进当前方案、还是修 bug。

整个闭环完全自动，人不用插手。

---

## 【第 4 页 · 方法】

把 ML-Master 用到 KuaiRand 上，我们做了三层工作：

**第一层，搭建任务。** 写日期切分、GAUC 和 nDCG@5 的评测、提交格式。

**第二层，给线索、不给答案。** 把官方给的改进方向和我们验证过的参考实现喂给 Agent，但具体怎么改进，全部留给 Agent 自己。

**第三层，独立验证。** 每一步都用验证集打分，最终提交用官方脚本校验。

---

## 【第 5 页 · 最终方案】

最精彩的是 Agent **自己摸索出来**的方案。

我们一开始喂给它的是深度模型——AutoInt 的自注意力、DIN 的序列建模。但 Agent 在迭代中自己发现，真正有效的是一个更朴素的组合：**LightGBM 的 LambdaRank 排序损失，加上"只看过去"的历史统计特征**。

这张图就是最终方案的四步流水线：原始日志 → 历史特征 → LambdaRank → 排序分数。

---

## 【第 6 页 · 结果】

结果是**全面超过基线**。测试集上，GAUC 提高 0.0207，nDCG@5 提高 0.0141，主分提高 0.0174——这是基线自身波动的 20 倍，统计上非常显著。

---

## 【第 7 页 · 亮点一 · 完全自主】

第一个亮点是**完全自主**。50 轮迭代，零人工干预。

这张图是 Agent 的迭代轨迹，可以看到它从接近随机一路爬升，最终自己冲过了基线。改进方向全是它自己提的，这正好是题目"自主性"评分最看重的。

---

## 【第 8 页 · 亮点二 · 防泄漏】

第二个亮点是**防标签泄漏**。

Agent 曾经自己学会用"点击"这种同期反馈信号去作弊，分数虚高到 0.84，甚至超过了理论上限。我们在任务描述里明确禁止了这种用法，保证最终分数是**真实可信**的。

---

## 【第 9 页 · 亮点三 · 负结果】

第三个是**负结果也有价值**。

Agent 试过给历史加时间衰减，结果模型自己把衰减学成了负数——说明用户的长期偏好，比近期行为更重要。这个发现我们也写进了报告。

---

## 【第 10 页 · 挑战】

过程里也踩了不少坑：DeepSeek 默认的"思考模式"会烧光 token，关掉之后单次调用从 287 秒降到 21 秒；还有刚才说的标签泄漏问题。

---

## 【第 11 页 · 总结 + 代码结构】

最后总结一下，顺便介绍我们的**代码结构**。所有代码都开源在 GitHub 上，仓库是这样组织的：

**根目录**是四份文档——README 总览、项目介绍、迭代日志、结果汇总。`final_submission.py` 是最终方案，`submission` 目录里是最终提交文件。

**helpers 目录**是官方给的评测和数据工具，**references 目录**是我们喂给 Agent 的两个参考实现，**mlmaster 目录**是整个 Agent 框架的源码。

这个结构让评委和复现者一眼就能看懂：**框架是主体，方案是产物，文档是补充**。

谢谢大家。

---

## 附：GitHub 结构速览（备用，讲的时候可照着指）

```
├── README.md              # 项目总览 + 安装 + 复现 + 分工
├── ABOUT_PROJECT.md       # Devpost 项目介绍
├── ITERATION_LOG.md       # 50 轮迭代日志
├── RESULTS_SUMMARY.md     # 结果表 + 资源使用报告
├── final_submission.py    # 最终方案（LightGBM LambdaRank）
├── submission/
│   └── submission.csv     # 最终提交（test 0.6120）
├── instructions.txt       # agent 读的任务描述
├── helpers/               # 官方 evaluate / data / submit / baseline
├── references/            # DIN + AutoInt 参考实现
├── mlmaster/              # ML-Master 框架源码（agent 本体）
└── requirements.txt
```
